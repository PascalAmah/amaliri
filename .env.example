# Contact form — Web3Forms (https://web3forms.com)
# Create a free access key and paste it into your local .env (NOT this example file).
# Without a key, the contact section shows an email fallback instead of the form.
PUBLIC_WEB3FORMS_KEY=

# Optional: enable Vercel Web Analytics
# PUBLIC_VERCEL_ANALYTICS=true
